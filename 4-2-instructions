In the C programming language to write a function that receives a pointer to a linked list of structures 
node type and returns a pointer to a duplicate list. The returned list should contain duplicates structures 
from the received list in the same order.
